1, On -Premises this overal the physical and handle the clint only
2, Software as a service this is revarse the On-Premises Handle the AWS ovaral using the lifestyle
3, platform as a service this is clint said requirement only application and runtime then handle in AWS using elaticbeanstac
4,Infrastrure as a service using the EC2 handles of clint and vartuval is handles in AWS



GLOBLE INFRA -  39 REGIONS AND 123availble zones
cdn end points using less latency
